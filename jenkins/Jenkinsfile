pipeline {
    agent any
    environment {
        POM_VERSION = ""
    }
    stages {
        stage('Build') {
            steps{
                sh 'mvn -Dmaven.test.failure.ignore=true install'
            }
        }
    }
}